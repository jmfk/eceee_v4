services:
  # Project-owned stateless image proxy. PostgreSQL, Redis, and MinIO are
  # provided by ../shared-local-infrastructure on OrbStack.
  imgproxy:
    image: darthsim/imgproxy:latest
    platform: linux/arm64
    container_name: eceee-v4-imgproxy
    restart: "no"
    environment:
      IMGPROXY_ALLOW_UNSAFE_URL: true
      IMGPROXY_KEY: ${IMGPROXY_KEY:?Run make configure-local-infra}
      IMGPROXY_SALT: ${IMGPROXY_SALT:?Run make configure-local-infra}
      IMGPROXY_MAX_SRC_RESOLUTION: 50.0
      IMGPROXY_MAX_SRC_FILE_SIZE: 52428800
      IMGPROXY_WORKERS: 4
      IMGPROXY_MAX_CLIENTS: 2048
      IMGPROXY_TTL: 31536000
      IMGPROXY_QUALITY: 85
      IMGPROXY_FORMAT_QUALITY: "jpeg=85,webp=85,avif=85"
      IMGPROXY_AUTO_ROTATE: true
      IMGPROXY_STRIP_METADATA: true
      IMGPROXY_STRIP_COLOR_PROFILE: false
      IMGPROXY_AUTO_WEBP: true
      IMGPROXY_AUTO_AVIF: true
      IMGPROXY_ENFORCE_WEBP: false
      IMGPROXY_ENFORCE_AVIF: false
      IMGPROXY_USE_S3: false
      IMGPROXY_SET_CANONICAL_HEADER: true
      IMGPROXY_ALLOWED_SOURCES: "https://eu-central-1.linodeobjects.com/,s3://eceee-media/,local://,http://host.docker.internal:10302/,http://test-minio:9000/,http://backend:8000/,http://eceee-v4-backend-editor:8000/,http://localhost:8080/"
      IMGPROXY_LOG_LEVEL: info
      IMGPROXY_ENABLE_DEBUG_HEADERS: "true"
      IMGPROXY_IGNORE_SSL_VERIFICATION: "true"
      IMGPROXY_DEVELOPMENT_ERRORS_MODE: "true"
      IMGPROXY_ALLOW_SECURITY_OPTIONS: "true"
      IMGPROXY_PRESETS: "thumbnail=resize:fill:150:150/gravity:sm,small=resize:fit:300:300,medium=resize:fit:600:600,large=resize:fit:1200:1200,hero=resize:fill:1920:1080/gravity:sm,avatar=resize:fill:128:128/gravity:sm/format:webp"
    ports:
      - "127.0.0.1:${ECEEE_IMGPROXY_PORT:-10106}:8080"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "bash", "-c", "</dev/tcp/127.0.0.1/8080"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
