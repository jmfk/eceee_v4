---
id: widget-two-columns-widget
title: Two columns widget
summary: Place child widgets into left and right columns.
order: 121
sectionId: widgets
sectionTitle: Widgets
sectionSummary: Add, edit, move, copy, hide, and remove widgets in page and object editors.
sectionOrder: 2
---

# Two columns widget

Place child widgets into left and right columns.

<!-- narration: Use the Two Columns widget when a section needs paired content areas. -->
<!-- goal: In this walkthrough, we add the Two columns widget to a page slot and open its settings. -->
<!-- why: This matters because each widget type has its own purpose, and choosing the right widget keeps the page easier to maintain. -->
<!-- outcome: After this walkthrough, you should know where to add the Two columns widget and where to continue configuring it. -->

1. Add or edit a Two Columns widget.
2. Configure the column ratio or layout options.
3. Add child widgets to the left and right slots.
4. Keep the two columns visually balanced.
5. Preview how the columns stack on mobile.

```video
[
  { "type": "goto", "path": "/pages/101/edit/slots", "caption": "Start in All Slots on a page editor so you can add the Two columns widget in context." },
  { "type": "click", "selector": "[data-testid='page-slot-toggle-content']", "caption": "Expand the Main Content slot so its widget controls are visible." },
  { "type": "click", "selector": "[data-testid='page-slot-add-content']", "caption": "Click the plus icon on the Main Content slot." },
  { "type": "click", "selector": "[data-testid='page-widget-option-easy-widgets-twocolumnswidget']", "caption": "Choose Two columns widget from the widget picker." },
  { "type": "click", "selector": "[data-testid='page-widget-edit-easy-widgets-twocolumnswidget']", "caption": "Open the settings panel for the new Two columns widget." },
  { "type": "caption", "caption": "Configure the fields, then save and preview the page before publishing.", "ms": 1200 }
]
```
